# Infrastructure

Deployment and environment configuration.

- `database/` — database schemas and migrations
- `storage/` — file and model storage configuration
- `deployment/` — hosting and deployment configuration
